'use client';

import { createTheme } from '@mui/material/styles';

// MUI v7.3.9 — compatible with Next.js 16 App Router
const theme = createTheme({
  palette: {
    mode: 'dark',
  },
});

export default theme;
